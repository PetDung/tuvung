spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://localhost:5432/agri_trace_db?reWriteBatchedInserts=true
    username: agri_admin
    password: change_me_in_production
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: create

jwt:
  secret: ${JWT_SECRET:AgriTraceSecretKeyForJWTTokenGeneration2024VeryLongSecretKey}
  expiration: 86400000

gateway:
  url: http://localhost:4000
